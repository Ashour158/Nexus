apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-workflow.fullname" . }}
  labels:
    {{- include "nexus-workflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-workflow.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-router-coprocessor.fullname" . }}
  labels:
    {{- include "nexus-router-coprocessor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-router-coprocessor.selectorLabels" . | nindent 4 }}

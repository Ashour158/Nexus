apiVersion: apps/v1
kind: Deployment
metadata:
  name: comm-service
  namespace: nexus
  labels:
    app.kubernetes.io/name: nexus-comm-service
    app.kubernetes.io/component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: nexus-comm-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: nexus-comm-service
        app.kubernetes.io/component: backend
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
      - name: comm-service
        image: ghcr.io/nexus-crm/nexus-comm-service:latest
        ports:
        - containerPort: 3009
          name: http
        env:
        - name: PORT
          value: "3009"
        - name: SERVICE_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: nexus-secrets
              key: comm-service-database-url
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: nexus-secrets
              key: redis-url
        - name: KAFKA_BROKERS
          valueFrom:
            configMapKeyRef:
              name: nexus-config
              key: KAFKA_BROKERS
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3009
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 3009
          initialDelaySeconds: 5
          periodSeconds: 5
        securityContext:
          readOnlyRootFilesystem: false
          allowPrivilegeEscalation: false
---
apiVersion: v1
kind: Service
metadata:
  name: comm-service
  namespace: nexus
  labels:
    app.kubernetes.io/name: nexus-comm-service
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nexus-comm-service
  ports:
  - name: http
    port: 3009
    targetPort: 3009

apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-contacts.fullname" . }}
  labels:
    {{- include "nexus-contacts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-contacts.selectorLabels" . | nindent 4 }}

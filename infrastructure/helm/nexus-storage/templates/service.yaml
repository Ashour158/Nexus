apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-storage.fullname" . }}
  labels:
    {{- include "nexus-storage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-storage.selectorLabels" . | nindent 4 }}

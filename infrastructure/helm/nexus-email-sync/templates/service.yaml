apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-email-sync.fullname" . }}
  labels:
    {{- include "nexus-email-sync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-email-sync.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-blueprint.fullname" . }}
  labels:
    {{- include "nexus-blueprint.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-blueprint.selectorLabels" . | nindent 4 }}

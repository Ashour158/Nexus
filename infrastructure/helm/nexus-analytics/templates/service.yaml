apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-analytics.fullname" . }}
  labels:
    {{- include "nexus-analytics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-analytics.selectorLabels" . | nindent 4 }}

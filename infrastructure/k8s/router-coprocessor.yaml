apiVersion: apps/v1
kind: Deployment
metadata:
  name: router-coprocessor
  namespace: nexus
  labels:
    app.kubernetes.io/name: nexus-router-coprocessor
    app.kubernetes.io/component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: nexus-router-coprocessor
  template:
    metadata:
      labels:
        app.kubernetes.io/name: nexus-router-coprocessor
        app.kubernetes.io/component: backend
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
      - name: router-coprocessor
        image: ghcr.io/nexus-crm/nexus-router-coprocessor:latest
        ports:
        - containerPort: 4001
          name: http
        env:
        - name: PORT
          value: "4001"
        - name: SERVICE_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: nexus-secrets
              key: router-coprocessor-database-url
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: nexus-secrets
              key: redis-url
        - name: KAFKA_BROKERS
          valueFrom:
            configMapKeyRef:
              name: nexus-config
              key: KAFKA_BROKERS
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 4001
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 4001
          initialDelaySeconds: 5
          periodSeconds: 5
        securityContext:
          readOnlyRootFilesystem: false
          allowPrivilegeEscalation: false
---
apiVersion: v1
kind: Service
metadata:
  name: router-coprocessor
  namespace: nexus
  labels:
    app.kubernetes.io/name: nexus-router-coprocessor
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nexus-router-coprocessor
  ports:
  - name: http
    port: 4001
    targetPort: 4001

apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-deals.fullname" . }}
  labels:
    {{- include "nexus-deals.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-deals.selectorLabels" . | nindent 4 }}

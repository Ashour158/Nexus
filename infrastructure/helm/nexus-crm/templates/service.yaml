apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-crm.fullname" . }}
  labels:
    {{- include "nexus-crm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-crm.selectorLabels" . | nindent 4 }}

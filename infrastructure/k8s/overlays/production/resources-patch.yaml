apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexus-crm
spec:
  template:
    spec:
      containers:
        - name: nexus-crm
          resources:
            limits:
              cpu: 4000m
              memory: 4Gi
            requests:
              cpu: 1000m
              memory: 2Gi

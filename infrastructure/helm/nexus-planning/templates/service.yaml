apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-planning.fullname" . }}
  labels:
    {{- include "nexus-planning.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-planning.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-comm.fullname" . }}
  labels:
    {{- include "nexus-comm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-comm.selectorLabels" . | nindent 4 }}

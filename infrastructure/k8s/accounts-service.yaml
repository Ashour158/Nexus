apiVersion: apps/v1
kind: Deployment
metadata:
  name: accounts-service
  namespace: nexus
  labels:
    app: accounts-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: accounts-service
  template:
    metadata:
      labels:
        app: accounts-service
    spec:
      containers:
      - name: accounts-service
        image: nexus/accounts-service:latest
        ports:
        - containerPort: 3031
          name: http
        env:
        - name: PORT
          value: "3031"
        - name: ACCOUNTS_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: nexus-secrets
              key: accounts-database-url
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: nexus-redis
              key: url
        - name: KAFKA_BROKERS
          valueFrom:
            configMapKeyRef:
              name: nexus-config
              key: KAFKA_BROKERS
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3031
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3031
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: accounts-service
  namespace: nexus
spec:
  selector:
    app: accounts-service
  ports:
  - port: 3031
    targetPort: 3031

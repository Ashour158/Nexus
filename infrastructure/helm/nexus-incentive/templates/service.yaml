apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-incentive.fullname" . }}
  labels:
    {{- include "nexus-incentive.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-incentive.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-notes.fullname" . }}
  labels:
    {{- include "nexus-notes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-notes.selectorLabels" . | nindent 4 }}

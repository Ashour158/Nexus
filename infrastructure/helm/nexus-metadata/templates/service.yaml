apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-metadata.fullname" . }}
  labels:
    {{- include "nexus-metadata.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-metadata.selectorLabels" . | nindent 4 }}

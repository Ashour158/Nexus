apiVersion: apps/v1
kind: Deployment
metadata:
  name: quotes-service
  namespace: nexus
  labels:
    app: quotes-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: quotes-service
  template:
    metadata:
      labels:
        app: quotes-service
    spec:
      containers:
      - name: quotes-service
        image: nexus/quotes-service:latest
        ports:
        - containerPort: 3033
          name: http
        env:
        - name: PORT
          value: "3033"
        - name: QUOTES_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: nexus-secrets
              key: quotes-database-url
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: nexus-redis
              key: url
        - name: KAFKA_BROKERS
          valueFrom:
            configMapKeyRef:
              name: nexus-config
              key: KAFKA_BROKERS
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3033
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3033
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: quotes-service
  namespace: nexus
spec:
  selector:
    app: quotes-service
  ports:
  - port: 3033
    targetPort: 3033

apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-notification.fullname" . }}
  labels:
    {{- include "nexus-notification.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-notification.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-auth.fullname" . }}
  labels:
    {{- include "nexus-auth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-auth.selectorLabels" . | nindent 4 }}

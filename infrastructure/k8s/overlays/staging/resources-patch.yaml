apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexus-crm
spec:
  template:
    spec:
      containers:
        - name: nexus-crm
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 128Mi

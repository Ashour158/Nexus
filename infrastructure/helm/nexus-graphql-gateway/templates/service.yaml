apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-graphql-gateway.fullname" . }}
  labels:
    {{- include "nexus-graphql-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-graphql-gateway.selectorLabels" . | nindent 4 }}

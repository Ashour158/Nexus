apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-quotes.fullname" . }}
  labels:
    {{- include "nexus-quotes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-quotes.selectorLabels" . | nindent 4 }}

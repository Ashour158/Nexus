apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexus-crm
spec:
  template:
    spec:
      containers:
        - name: nexus-crm
          resources:
            limits:
              cpu: 250m
              memory: 256Mi
            requests:
              cpu: 50m
              memory: 64Mi

apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-search.fullname" . }}
  labels:
    {{- include "nexus-search.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-search.selectorLabels" . | nindent 4 }}

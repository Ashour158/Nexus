apiVersion: apps/v1
kind: Deployment
metadata:
  name: knowledge-service
  namespace: nexus
  labels:
    app.kubernetes.io/name: nexus-knowledge-service
    app.kubernetes.io/component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: nexus-knowledge-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: nexus-knowledge-service
        app.kubernetes.io/component: backend
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
      - name: knowledge-service
        image: ghcr.io/nexus-crm/nexus-knowledge-service:latest
        ports:
        - containerPort: 3023
          name: http
        env:
        - name: PORT
          value: "3023"
        - name: SERVICE_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: nexus-secrets
              key: knowledge-service-database-url
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: nexus-secrets
              key: redis-url
        - name: KAFKA_BROKERS
          valueFrom:
            configMapKeyRef:
              name: nexus-config
              key: KAFKA_BROKERS
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3023
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 3023
          initialDelaySeconds: 5
          periodSeconds: 5
        securityContext:
          readOnlyRootFilesystem: false
          allowPrivilegeEscalation: false
---
apiVersion: v1
kind: Service
metadata:
  name: knowledge-service
  namespace: nexus
  labels:
    app.kubernetes.io/name: nexus-knowledge-service
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nexus-knowledge-service
  ports:
  - name: http
    port: 3023
    targetPort: 3023

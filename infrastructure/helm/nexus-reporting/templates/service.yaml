apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-reporting.fullname" . }}
  labels:
    {{- include "nexus-reporting.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-reporting.selectorLabels" . | nindent 4 }}

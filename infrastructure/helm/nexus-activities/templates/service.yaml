apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus-activities.fullname" . }}
  labels:
    {{- include "nexus-activities.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus-activities.selectorLabels" . | nindent 4 }}

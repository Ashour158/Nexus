# SOPS creation rules. The age recipient below is public and safe to commit.
# Replace the placeholder before encrypting. For recipient/key rotation, SOPS
# accepts multiple age recipients as a comma-separated string in this field.
creation_rules:
  - path_regex: secrets/prod\.env\.sops\.env$
    age: 'REPLACE_WITH_YOUR_AGE_PUBLIC_KEY'
